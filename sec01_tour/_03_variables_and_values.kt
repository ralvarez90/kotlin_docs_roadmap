/**
 * VARIABLES AND VALUES
 *
 * The variables are identifiers that store references in memory. All
 * variables have a data type that is maintained throughout the execution
 * of the program.
 *
 * We define variables using keywords var and val and use var to create
 * mutable reference variables and val to create immutable reference
 * variables.
 */
fun main() {
    val welcomeMsg = "Welcome to Kotlin"
    println(welcomeMsg)

    // raise an error
    // welcomeMsg = welcomeMsg.uppercase()
}